============
Installation
============

At the command line::

    $ easy_install dj-prosftpd

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-prosftpd
    $ pip install dj-prosftpd
